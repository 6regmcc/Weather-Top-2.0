<div class="ui segment" id="charttemp"></div>
<script>
  data = {
    labels: [
      {{#each data.trendData.arrDates}}
        "{{getLocalTime this}}",
      {{/each}}
    ],
    datasets: [{
      name: "Wind Speed", type: "line",
      values: [
        {{#each data.trendData.arrTemp}}
          "{{this}}",
        {{/each}}
      ]
    }
    ]
  };
  new frappe.Chart("#charttemp", {
    data: data,
    type: "line",
    height: 180,
    colors: ["red"]
  });
</script>